import React from 'react';

const Config = ( props ) => {
    return (
        <div>
            <ul>
                <li><h2>Price: { props.price }</h2></li>
            </ul>
        </div>
    );
};

export default Config;